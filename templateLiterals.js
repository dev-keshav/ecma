console.log("This will not work when we break the line, if you want to do, we use + operator like this" +
"This kind of thing");


var a=69;

function fullname() {
    let user= "Developer";
    return user;
}
console.log(`This is the back tics
by which we can break the line ${a} hello ${fullname()}`);